# API key for authentication
API_KEY=your-secure-api-key-here

# Port to run server on (default: 8080)
PORT=8080

# Optional: Google Cloud project ID (if not using default)
# GOOGLE_CLOUD_PROJECT=your-project-id